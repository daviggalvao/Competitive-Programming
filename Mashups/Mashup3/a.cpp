#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    int t;
    cin >> t;
    while(t--){
        int n,m,a,b,sum=0;
        bool row = true;
        cin >> n >> m >> a >> b;
        n--; m--; a--; b--;
        
        cout << sum << '\n';
    }
}